package poc.minimize;

import static com.google.common.base.Preconditions.*;

class SimplificadorExpressao {

    public SimplificadorExpressao(TabelaVerdade tabela) {
        TabelaVerdade _tabela = checkNotNull(tabela);
    }
}
